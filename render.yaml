services:
  - type: web
    name: whatsapp-bot
    env: go
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: PORT
        value: 8080
      - key: API_KEY
        value: your-secret-api-key-2024
