services:
  - type: web
    name: whatsapp-bot
    runtime: go
    plan: free
    buildCommand: go build -o main .
    startCommand: ./main
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8080
      - key: API_KEY
        value: your-secret-api-key-2024
